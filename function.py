def print_list(lst):
    print(lst)

def find_max(a, b, c):
    maximum = max(a, b, c)
    print(maximum)
    return maximum

def find_min(*nums):
    minimum = min(nums)
    print(max(nums))
    return minimum

def find_max_in_list(lst):
    maximum = max(lst)
    return maximum

def find_min_in_list(lst):
    minimum = min(lst)
    return minimum

def sum_list(lst):
    total = sum(lst)
    return total

def average_list(lst):
    average = sum(lst) / len(lst)
    return average